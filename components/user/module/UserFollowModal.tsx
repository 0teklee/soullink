import React from "react";
import { UserType } from "@/libs/types/common/userType";

const UserFollowModal = ({ follows }: { follows: UserType[] }) => {
  const sample = null;
  return (
    <div>
      <></>
    </div>
  );
};

export default UserFollowModal;
